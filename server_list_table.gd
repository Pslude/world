extends "res://table.gd"

#func Render():
	#for row_data in data:
		#var row = TableRow.instantiate()
		#$Rows.add_child(row)
		#
		#var cell = TableCell.instantiate()
		#cell.text = str(row_data['name'])
		#row.add_child(cell)
		#
		#var cell2 = TableCell.instantiate()
		#cell2.text = '<button>join'
		#row.add_child(cell2)
		#
		##for key in row_data:
			##var cell = TableCell.instantiate()
			##cell.text = str(row_data[key])
			##row.add_child(cell)
